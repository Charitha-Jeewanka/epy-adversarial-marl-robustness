"""Budget/epsilon-ball invariants for attacks (GEMINI.md §9).
Fill in once a concrete BaseAttack subclass exists."""
import pytest


@pytest.mark.skip(reason="Enable when a concrete attack is implemented.")
def test_budget_never_exceeded() -> None:
    ...


@pytest.mark.skip(reason="Enable when a concrete attack is implemented.")
def test_perturbation_within_epsilon_ball() -> None:
    ...
